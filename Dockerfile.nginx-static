# Simple nginx Docker setup for static files
FROM nginx:alpine

# Copy static files to nginx serve directory
COPY . /usr/share/nginx/html/

# Expose port 80
EXPOSE 80

# Start nginx
CMD ["nginx", "-g", "daemon off;"]
